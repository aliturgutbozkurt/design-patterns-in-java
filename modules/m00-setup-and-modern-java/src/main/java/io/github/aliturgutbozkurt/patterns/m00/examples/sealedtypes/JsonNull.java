package io.github.aliturgutbozkurt.patterns.m00.examples.sealedtypes;

/** JSON {@code null}. An enum with one constant: there is exactly one null value. */
public enum JsonNull implements Json {
    INSTANCE
}
